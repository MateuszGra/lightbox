const lightboxOne = new Lightbox({
    selector: '.iframe',
    type: 'iframe',
});

const lightboxTwo = new Lightbox({
  selector: '.iframe2',
  type: 'iframe',
});


const lightboxThree = new Lightbox({
  selector: '.image',
  type: 'image',
});